<?xml version="1.0"?>
<command>
  <name>med</name>
  <id>bgp_options_med</id>
  <context>bgp options</context>
  <parameters>
    <parameter>
      <name>med-type</name>
      <description>new MED behaviour</description>
    </parameter>
  </parameters>
  <abstract>set the behaviour of the BGP decision process regarding the MED</abstract>
  <description>
<p>
This command changes the behaviour of the MED-based rule of the BGP decision process. Two modes are currently supported: <b>deterministic</b> and <b>always-compare</b>.
</p>
<p>
If the <b>deterministic</b> mode is selected, then the MED-based rule will only compare the MED value of routes received from the same AS.
</p>
<p>
If the <b>always-compare</b> mode is selected, then the MED-based rule will compare the MEd value of routes even if they are received from different neighbor ASes.
</p>
  </description>
  
  
</command>
